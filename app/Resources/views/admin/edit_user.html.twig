{% extends 'base.html.twig' %}

{% block title %}
    {{ 'page.edit_user'|trans }}
{% endblock %}

{% block body %}
    {% include 'login_panel.html.twig' %}
    <h1>{{ 'page.edit_user'|trans }}</h1>

    {{ form_start(form, { method: 'post'}) }}   
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}