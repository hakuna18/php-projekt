{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.book_add'|trans }}
{% endblock %}

{% block body %}
{% include 'top_menu.html.twig' %}

    <h1>
        {{ 'title.book_add'|trans }}
    </h1>

    {{ form_start(form, { method: 'post' }) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}