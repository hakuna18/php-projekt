{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.book_edit'|trans }}
{% endblock %}

{% block body %}
{% include 'top_menu.html.twig' %}

    <h1>
        {{ 'title.book_edit'|trans }}
    </h1>

    <div>
        <img src="{{asset('uploads/covers/' ~ book.cover)}}" width=100 height=150>
    </div>
    {{ form_start(form, { method: 'post' }) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}